sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: JSON format
    deactivate server
    
    Rerenders the note list on the page and sends the new note to the serve instead of rerendering the complete page.

